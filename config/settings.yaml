# 组件配置
# enabled_components: 启用的组件列表（注释掉的不加载）
enabled_components:

# 窗口配置
window:
  width: 1200
  height: 800
  # 是否移除原生标题栏（true: 无标题栏, false: 显示原生标题栏）
  remove_titlebar: true
  # 是否允许通过窗口边缘拖动调整大小（仅在 remove_titlebar 为 true 时有效）
  resizable_border: true
